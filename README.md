# s3Flood - Modern S3 Load Testing Tool

> 🚧 **Активная разработка:** Ветка `feature/cli-min` - полная реорганизация в современный CLI-инструмент

## 🎯 Цели проекта

Создание легковесного, кроссплатформенного инструмента для нагрузочного тестирования S3-совместимых бэкендов (MinIO, AWS S3, etc.) с фокусом на:

- **Простота использования:** Единый консольный инструмент для Linux/Windows
- **Гибкость:** Профили нагрузки (read/write/mixed), поддержка различных клиентов (aws-cli, s5cmd, rclone)
- **Прозрачность:** Чёткий вывод метрик, детальные отчёты (CSV/JSON)
- **Надёжность:** Retry механизмы, таймауты, корректная обработка ошибок
- **CI/CD ready:** GitHub Actions для автоматизированного тестирования

---

## 📋 Roadmap

### 🎉 Этап 1: Минимальный CLI и архитектура ✅ В процессе

**Статус:** 30% завершено

#### Выполнено ✅
- [x] Создана ветка `feature/cli-min`
- [x] Базовая структура `src/s3flood/`
- [x] `pyproject.toml` с зависимостями
- [x] Исправление работы с симлинками на macOS

#### В работе 🔄
- [ ] **Runners (исполнители команд)**
  - [ ] `runners/base.py` - базовый интерфейс `Runner`
  - [ ] `runners/s5cmd.py` - первая реализация
  - [ ] `runners/awscli.py` - AWS CLI backend
  - [ ] `runners/rclone.py` - rclone backend

- [ ] **Основные модули**
  - [ ] `dataset.py` - генерация/очистка тестовых файлов
  - [ ] `profiles.py` - профили нагрузки (write-heavy, read-heavy, mixed)
  - [ ] `executor.py` - планировщик задач, многопоточность, retry
  - [ ] `metrics.py` - сбор throughput/latency, экспорт CSV/JSON
  - [ ] `cli.py` - CLI интерфейс (argparse/click)
  - [ ] `config.py` - валидация через pydantic

- [ ] **Тестирование**
  - [ ] Базовые юнит-тесты (pytest)
  - [ ] Моки для runners
  - [ ] Linux CI с MinIO (GitHub Actions)

---

### 🚀 Этап 2: Полная функциональность клиентов

**Статус:** Планируется

- [ ] Полная реализация `rclone` runner
- [ ] Полная реализация `awscli` runner
- [ ] Расширенные профили:
  - [ ] `read-heavy` (90/10 download/upload)
  - [ ] `mixed-70-30` (70/30 upload/download)
  - [ ] `single-thread` vs `multi-thread`
  - [ ] `sustained` vs `bursty` patterns
- [ ] Warmup период (`--warmup`)
- [ ] Windows job в CI (ограниченный сценарий)
- [ ] Интеграционные тесты с реальными S3 бэкендами

---

### 📚 Этап 3: Документация и удобство

**Статус:** Планируется

- [ ] Примеры конфигов в `examples/configs/`
- [ ] Полная документация в `docs/`:
  - [ ] Quickstart для Linux/Windows
  - [ ] Описание профилей
  - [ ] Гайд по клиентам
- [ ] PyInstaller сборки (опционально)
- [ ] Release workflow для автоматических релизов

---

### ⚡ Этап 4: Оптимизация и продвинутые фичи

**Статус:** Планируется

- [ ] Пул задач с управлением очередями
- [ ] Ограничение IOPS/throughput
- [ ] Распределения размеров файлов (Pareto/Zipf)
- [ ] Улучшенное логирование
- [ ] TUI интерфейс (опционально, через rich/textual)
- [ ] Экспорт метрик в Prometheus формат

---

## 🏗️ Целевая архитектура

```
s3Flood/
├── pyproject.toml          # Build система, зависимости, настройки линтеров
├── README.md               # Этот файл
├── src/
│   └── s3flood/
│       ├── cli.py          # CLI точка входа (dataset, run команды)
│       ├── config.py       # Pydantic модели конфигурации
│       ├── dataset.py      # Генерация/очистка тестовых файлов
│       ├── profiles.py     # Профили нагрузки
│       ├── executor.py     # Планировщик задач, потоки, retry
│       ├── metrics.py      # Сбор и экспорт метрик
│       ├── tui.py          # TUI (опционально)
│       └── runners/
│           ├── base.py     # Абстрактный Runner интерфейс
│           ├── awscli.py   # AWS CLI backend
│           ├── s5cmd.py    # s5cmd backend
│           └── rclone.py   # rclone backend
├── tests/                  # pytest тесты
├── examples/
│   └── configs/            # Готовые профили нагрузки
├── docs/                   # Детальная документация
└── .github/
    └── workflows/
        └── ci.yml          # Linux/Windows матрица тестов
```

---

## 🎮 Примеры использования (целевой API)

### Генерация датасета

```bash
# Создать тестовые файлы
s3flood dataset create \
  --sizes small=1MBx100,medium=64MBx50,large=512MBx10 \
  --path ./data

# Очистка
s3flood dataset clean --path ./data
```

### Запуск нагрузочного теста

```bash
# Write-heavy профиль с s5cmd
s3flood run \
  --profile write-heavy \
  --client s5cmd \
  --threads 8 \
  --endpoint http://localhost:9000 \
  --bucket test-bucket \
  --access-key minioadmin \
  --secret-key minioadmin

# Read-heavy с rclone
s3flood run \
  --profile read-heavy \
  --client rclone \
  --threads 16 \
  --endpoint http://localhost:9000 \
  --bucket test-bucket

# Кастомный микс операций
s3flood run \
  --ops upload,download,delete \
  --mix 50,50,0 \
  --duration 300s \
  --report out.json \
  --metrics out.csv
```

---

## 📊 Профили нагрузки (встроенные)

| Профиль | Описание | Операции |
|---------|----------|----------|
| `write-heavy` | Преимущественно запись | 80% upload, 20% download |
| `read-heavy` | Преимущественно чтение | 90% download, 10% upload |
| `mixed-70-30` | Сбалансированный | 70% upload, 30% download |
| `single-thread` | Однопоточный | threads=1, последовательно |
| `multi-thread` | Многопоточный | threads=N, конкурентно |
| `sustained` | Постоянная нагрузка | Равномерное распределение |
| `bursty` | Импульсная нагрузка | Пачками с паузами |

---

## 🛠️ Плагины клиентов

Все клиенты реализуют единый интерфейс `Runner`:

```python
class Runner(ABC):
    @abstractmethod
    def prepare(self) -> bool: ...
    
    @abstractmethod
    def upload(self, file: Path, key: str) -> OperationResult: ...
    
    @abstractmethod
    def download(self, key: str, dest: Path) -> OperationResult: ...
    
    @abstractmethod
    def delete(self, key: str) -> OperationResult: ...
    
    @abstractmethod
    def healthcheck(self) -> bool: ...
```

### Поддерживаемые клиенты

- **aws-cli** (`--client awscli`) - AWS CLI через subprocess
- **s5cmd** (`--client s5cmd`) - Высокопроизводительный S3 клиент
- **rclone** (`--client rclone`) - Универсальный sync tool

---

## 📈 Метрики и отчёты

### Собираемые данные на операцию:
- `start_ts`, `end_ts` - временные метки
- `bytes` - размер данных
- `op` - тип операции (upload/download/delete)
- `client` - используемый клиент
- `status` - успех/ошибка
- `error` - текст ошибки (если есть)

### Агрегации:
- **Throughput:** B/s и MB/s
- **Latency:** mean, P50, P90, P99
- **Error rate:** процент неудачных операций

### Форматы экспорта:
- CSV (построчно, для анализа)
- JSON (итоговые агрегаты)
- Prometheus (опционально, text format)

---

## 🔧 CI/CD

GitHub Actions workflow:

- **Матрица:** `ubuntu-latest`, `windows-latest`
- **Python:** 3.10, 3.11, 3.12
- **Шаги:**
  1. Поднять MinIO (Docker на Linux)
  2. Запустить мини-профиль (малые файлы, 2 потока, 30-60 сек)
  3. Проверить артефакты (out.json, out.csv)
  4. Бейдж "build passing"

---

## 📦 Установка (текущая, временная)

До завершения рефакторинга:

```bash
# Клонировать ветку
git clone -b feature/cli-min https://github.com/dvorobiev/s3Flood.git
cd s3Flood

# Установить зависимости
pip install -e .

# Запуск (старый интерфейс пока доступен)
python s3_flood.py --help
```

---

## 🤝 Участие в разработке

Ветка `feature/cli-min` - активная разработка. Pull requests приветствуются!

**Приоритеты:**
1. Завершение этапа 1 (минимальный CLI)
2. Базовое покрытие тестами
3. Документация API

---

## 📄 Лицензия

MIT License - см. [LICENSE](LICENSE)

---

## 📞 Контакты

Создайте [Issue](https://github.com/dvorobiev/s3Flood/issues) для вопросов и предложений.
