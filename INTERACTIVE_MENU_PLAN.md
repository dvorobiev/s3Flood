# План интерактивного меню для s3flood

## Цель
Создать удобное TUI-меню для интерактивной работы с s3flood без необходимости помнить все CLI-флаги.

## Структура меню

### Главное меню
```
┌─────────────────────────────────────┐
│  s3flood — Interactive Menu         │
├─────────────────────────────────────┤
│  1. 🚀 Запустить тест              │
│  2. 📦 Создать датасет              │
│  3. ⚙️  Управление конфигами        │
│  4. 🔍 Проверить конфиг             │
│  5. 📋 Просмотр метрик              │
│  6. ❌ Выход                         │
└─────────────────────────────────────┘
```

### 1. Запустить тест
Подменю:
- Выбор профиля (write/read/mixed-70-30)
- Выбор клиента (awscli/rclone/s3cmd) — если поддерживается
- Выбор конфига (из списка существующих или создать новый)
- Быстрый запуск с последним конфигом
- Запуск с интерактивным вводом параметров

### 2. Создать датасет
**Форма с полями:**
- Путь к датасету (текстовое поле с автодополнением)
- Размер датасета (радиобаттоны: auto / вручную в GB/MB)
- Использовать симлинки? (чекбокс)
- Минимальные количества файлов (3 числовых поля: small, medium, large)
- Лимиты размеров файлов (3 текстовых поля: small, medium, large в формате "100MB", "1GB")
- Safety ratio (числовое поле, слайдер 0.1-1.0)

Все поля видны одновременно на экране, можно переключаться Tab/Shift+Tab

### 3. Управление конфигами
- Создать новый конфиг (форма редактирования)
- Редактировать существующий конфиг (форма с загруженными значениями)
- Копировать конфиг
- Удалить конфиг
- Просмотреть список конфигов

**Форма редактирования конфига:**
Все опции видны на экране одновременно:
- Профиль (радиобаттоны: write/read/mixed-70-30)
- Клиент (радиобаттоны: awscli/rclone/s3cmd)
- Endpoint (текстовое поле) или Endpoints (множественный ввод)
- Endpoint mode (радиобаттоны: round-robin/random)
- Bucket (текстовое поле)
- Access key / Secret key (текстовые поля, скрытый ввод)
- AWS profile (текстовое поле)
- Threads (числовое поле)
- Data dir (текстовое поле)
- Infinite (чекбокс)
- Mixed read ratio (числовое поле, слайдер 0.0-1.0)
- Pattern (радиобаттоны: sustained/bursty)
- Burst duration (числовое поле)
- Burst intensity multiplier (числовое поле)
- Queue limit (числовое поле, опционально)
- Max retries (числовое поле)
- Retry backoff base (числовое поле)
- Order (радиобаттоны: sequential/random)
- Unique remote names (чекбокс)

Навигация: Tab/Shift+Tab между полями, Enter для сохранения, Esc для отмены

### 4. Проверить конфиг
- Выбрать конфиг из списка
- Валидация структуры YAML
- Проверка обязательных полей
- Проверка доступности endpoint
- Проверка доступности bucket
- Проверка креденшлов

### 5. Просмотр метрик
- Выбрать CSV/JSON файл
- Показать последний отчёт
- Статистика по операциям
- Графики (если возможно)

## Технические детали

### Библиотека для TUI
Использовать `textual` для полноценных форм с чекбоксами, радиобаттонами и текстовыми полями.

**Почему textual:**
- Поддержка форм с чекбоксами, радиобаттонами, текстовыми полями
- Визуальное редактирование конфигов (все опции видны на экране)
- Удобная навигация по формам
- Хорошая документация и примеры

### Структура кода

```
src/s3flood/
  ├── cli.py              # Текущий CLI (оставить для совместимости)
  ├── interactive.py      # Новый модуль с интерактивным меню
  │   ├── menu.py         # Главное меню и навигация
  │   ├── dataset_wizard.py  # Мастер создания датасета
  │   ├── config_wizard.py   # Мастер создания/редактирования конфига
  │   ├── config_validator.py # Валидация конфигов
  │   └── metrics_viewer.py  # Просмотр метрик
  └── ...
```

### Этапы реализации

#### Этап 1: Базовая структура меню ✅
- [x] Создать `interactive.py` с главным меню
- [x] Интегрировать в CLI (новый флаг `--interactive` или `s3flood` без аргументов)
- [x] Базовые пункты меню (заглушки)

#### Этап 2: Мастер создания датасета ✅
- [x] Интерактивный ввод параметров
- [x] Валидация путей и размеров
- [x] Вызов существующей функции `plan_and_generate`

#### Этап 3: Мастер создания конфига
- [x] Пошаговый ввод основных параметров (без `profile`)
- [x] Сохранение в YAML
- [ ] Шаблоны для быстрого старта (готовые пресеты под MinIO/S3)

#### Этап 4: Конфиги и проверка
- [x] Интерактивный мастер создания нового конфига
- [x] Выбор и проверка конфига (список доступных файлов + ручной ввод)
- [x] Просмотр первых объектов в бакете для выбранного конфига
- [x] Безопасное удаление объектов из бакета (с двойным подтверждением и спиннером)
- [ ] Отдельный тест доступа (проверка креденшлов без удаления/чтения всего бакета)

#### Этап 5: Запуск теста через меню
- [x] Выбор конфига
- [x] Выбор профиля нагрузки (write/read/mixed) независимо от конфига
- [x] Переопределение параметров через меню
- [ ] Запуск с визуализацией прогресса

#### Этап 6: Просмотр метрик
- [ ] Парсинг CSV/JSON
- [ ] Красивое отображение статистики
- [ ] Фильтры и сортировка

## Совместимость

- CLI остаётся полностью рабочим (обратная совместимость)
- Новое меню активируется через `s3flood` без аргументов или `s3flood --interactive`
- Все существующие функции переиспользуются

## UI/UX принципы

1. **Простота**: минимум уровней вложенности
2. **Быстрый доступ**: горячие клавиши для частых операций
3. **Валидация**: проверка ввода на каждом шаге
4. **Отмена**: возможность вернуться назад на любом этапе
5. **Автодополнение**: для путей и имён файлов
6. **Цвета**: использовать `rich` для красивого оформления

